# Shared icon overrides for SVGIconFactory and DP5 tool icons.
# Place .svg or .png files named by icon ID.
# SVGIconFactory checks here first before using hardcoded SVG source.
# DP5 tool icons: pencil.svg, eraser.svg, fill.svg etc.
# Supported: .svg (preferred), .png
# Size: square recommended, 32x32 or 64x64
